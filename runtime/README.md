# Runtime

This directory is reserved for runtime provider implementations and shared execution contracts.

Planned contents:

- provider abstraction
- Docker execution backend for `v0.1`
- provider-specific sandbox profiles
- execution metadata and lifecycle hooks
