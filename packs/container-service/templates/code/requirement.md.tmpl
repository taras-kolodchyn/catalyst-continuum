# {{task.title}}

Generated from `{{source_ref.primary}}`.

## Description

{{task.description}}

## Suggested Integration

- Keep `src/features/{{source_ref.ident}}.rs` aligned with `requirements/{{source_ref.slug}}.json`.
- Expose the generated slice through the `/requirements` endpoint.
- Review acceptance criteria for `{{source_ref.value}}` before promoting the change.
