# {{repository.name}}

Generated by Catalyst Continuum from the `{{pack.id}}` pack.

## Overview

{{brief.summary}}

## Service Endpoints

- `GET /` returns repository and pack metadata together with the generated requirement count.
- `GET /healthz` returns a basic health payload for probes and smoke checks.
- `GET /requirements` returns the structured requirement catalog generated from the brief.

## Run Locally

```bash
cargo run
curl http://127.0.0.1:8080/healthz
curl http://127.0.0.1:8080/requirements
```

## Repository

- Owner: `{{repository.owner}}`
- Default branch: `{{repository.default_branch}}`
- Visibility: `{{repository.visibility}}`

## Next Steps

1. Extend the generated requirement handlers in `src/main.rs`.
2. Keep `requirements/*.json` aligned with the implementation slices under `src/features/`.
3. Add deeper tests and CI wiring for the generated container service.
