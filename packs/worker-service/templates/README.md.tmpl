# {{repository.name}}

Generated by Catalyst Continuum from the `{{pack.id}}` pack.

## Overview

{{brief.summary}}

## Commands

- `cargo run -- summary` returns repository and pack metadata together with the generated requirement count.
- `cargo run -- requirements` returns the structured requirement catalog generated from the brief.
- `cargo run -- run-once` simulates one worker pass across the generated requirement set.

## Run Locally

```bash
cargo run -- summary
cargo run -- requirements
cargo run -- run-once
```

## Repository

- Owner: `{{repository.owner}}`
- Default branch: `{{repository.default_branch}}`
- Visibility: `{{repository.visibility}}`

## Next Steps

1. Extend the generated worker routing in `src/main.rs`.
2. Keep `src/jobs/` aligned with `requirements/*.json`.
3. Replace the demo `run-once` flow with real queue or schedule integration.
